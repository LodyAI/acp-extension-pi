{
  "name": "acp-extension-pi",
  "version": "0.1.0",
  "description": "Lody-owned ACP adapter for the native Pi runtime",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "acp-extension-pi": "dist/index.js"
  },
  "files": [
    "dist",
    "native/*.node",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LodyAI/acp-extension-pi.git"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "build:native": "node scripts/build-native.mjs",
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "smoke": "node scripts/smoke.mjs",
    "format": "prettier --write .",
    "check": "pnpm typecheck && pnpm test && prettier --check .",
    "prepack": "pnpm build",
    "prepublishOnly": "node -e \"for (const arch of ['x64', 'arm64']) require('node:fs').accessSync('native/win32-' + arch + '.node')\""
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "1.3.0",
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@modelcontextprotocol/sdk": "1.29.0",
    "acp-extension-core": "0.1.0",
    "zod": "4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "node-gyp": "13.0.2",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
